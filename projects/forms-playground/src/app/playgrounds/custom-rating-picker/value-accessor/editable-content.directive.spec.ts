import { EditableContentValueAccessor } from './editable-content-value-accessor.directive';

describe('EditableContentDirective', () => {
  it('should create an instance', () => {
    const directive = new EditableContentValueAccessor();
    expect(directive).toBeTruthy();
  });
});
